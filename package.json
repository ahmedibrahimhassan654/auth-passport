{
  "name": "pasport-auth",
  "version": "1.0.0",
  "description": "pasport auth lessons",
  "main": "app.js",
  "scripts": {
    "test": "echo \"Error: no test specified\" && exit 1"
  },
  "author": "ahmed",
  "license": "ISC",
  "dependencies": {
    "ejs": "^3.0.1",
    "express": "^4.18.2",
    "passport": "^0.4.1",
    "passport-google-oauth2": "^0.2.0"
    
  },
  "devDependencies": {
    "nodemon": "^2.0.2"
  }
}
